# 限定棋盘抽卡模拟器 - 开发计划

## 一、技术选型与项目结构

**技术栈：**
- Python 3.x + Tkinter (GUI)
- PyInstaller (打包成单文件exe)
- UTF-8编码

**项目结构：**
```
抽卡模拟器/
├── main.py              # 主程序入口 + GUI界面
├── game_logic.py        # 核心游戏逻辑引擎
├── board_data.py        # 棋盘数据配置（格子、概率、角色池、皮肤）
├── 棋盘地图手动统计.csv # 棋盘数据源（唯一数据源）
└── build.bat            # 打包脚本
```

---

## 二、核心功能清单 ✅ 已完成

### 基础功能
- [x] 方形顺时针蛇形棋盘布局（54格主路径 + 2个9格分支）
- [x] 完整的概率暗箱机制（严格遵循官方规则）
- [x] S级保底系统（70变格 / 90硬保底）
- [x] 集点赠礼机制（每10次触发）
- [x] 变格机制（70抽后勇者宝箱高概率出S级）
- [x] 皮肤装扮礼遇系统（3种皮肤类型）

### 高级功能
- [x] 分支系统（S级角色区 + 皮肤惊喜区）
- [x] 两段式移动算法（分支离开时精确控制）
- [x] 5档速度控制（0.5x/1x/2x/4x/跳过）
- [x] 单次投掷 + 十连投掷
- [x] 骰子消耗系统（红骰 + 环石双货币）
- [x] PyInstaller单文件打包

---

## 三、棋盘数据格式规范

### 3.1 主路径编号规则（0-54）

| 编号 | 说明 | 示例 |
|------|------|------|
| **0** | 起点（独立，不参与循环） | `起点——0` |
| 1-15 | 主路径前半段 | `于此同行（海月）——1` |
| **16** | **分支1入口** | `学徒宝箱——16[分支1入口]` |
| **17** | **分支1跳过点** | `于此同行（翳）——17[跳过]` |
| **18** | **分支1出口** | `勇者宝箱——18[到达]` |
| 19-42 | 主路径中段 | `学徒宝箱——19` |
| **43** | **分支2入口** | `勇者宝箱——43[分支2入口]` |
| **44** | **分支2跳过点** | `于此同行（海月）——44[跳过]` |
| **45** | **分支2出口** | `迷迭棋盒（30个白色棋子）——45[结束]` |
| 46-53 | 主路径后半段 | `学徒宝箱——46` |
| **54** | 循环终点 | `勇者宝箱——54[终点]` |

### 3.2 分支编号规则

**分支1（S级角色区）：B1-0 到 B1-8（索引55-63）**
```
于此同行（浔）——B1-0[S级角色]
改装时刻·涂装——B1-1
勇者宝箱——B1-2
学徒宝箱——B1-3
勇者宝箱——B1-4
学徒宝箱——B1-5
勇者宝箱——B1-6
学徒宝箱——B1-7
勇者宝箱——B1-8
```

**分支2（皮肤惊喜区）：B2-0 到 B2-8（索引64-72）**
```
多重惊喜——B2-0
今日穿搭——B2-1
勇者宝箱——B2-2
勇者宝箱——B2-3
勇者宝箱——B2-4
勇者宝箱——B2-5
勇者宝箱——B2-6
勇者宝箱——B2-7
勇者宝箱——B2-8
```

### 3.3 特殊标记符号

| 符号 | 含义 | 使用场景 |
|------|------|---------|
| `[分支X入口]` | 分支触发点 | `学徒宝箱——16[分支1入口]` |
| `[跳过]` | 不进分支时跳到这里 | `翳同行——17[跳过]` |
| `[到达]` | 分支完成后返回这里 | `勇者宝箱——18[到达]` |
| `[结束]` | 分支结束位置 | `迷迭30——45[结束]` |
| `[终点]` | 循环终点 | `勇者宝箱——54[终点]` |
| `[S级角色]` | 特殊格子类型 | `浔同行——B1-0[S级角色]` |

---

## 四、走棋规则

### 正常移动
```
起点(0) → 走N点 → 按顺序前进
循环: 54号(终点) → 再掷骰子 → 从1号继续（不回起点）
```

### 分支触发规则

**分支1触发：**
```
到达16号 → 选择进入分支 → B1-0到B1-8 → 返回18号
路过16号    → 直接到17号（跳过分支）
```

**分支2触发：**
```
到达43号 → 选择进入分支 → B2-0到B2-8 → 返回45号
路过43号    → 直接到44号（跳过分支）
```

### 分支离开逻辑（两段式移动）

当在分支末尾投掷的点数超出分支范围时：
1. **段1**: 在分支内走到末尾
2. **跳转**: 从分支末尾跳转到出口
3. **段2**: 在主路径上继续移动剩余步数

**关键公式：**
```python
steps_to_exit = branch_end - current_pos  # 分支内还能走的步数
remaining_steps = dice_value - steps_to_exit - 1  # 主路径剩余步数
target_pos = _cycle_pos(exit_main_idx, remaining_steps)  # 最终目标位置
```

---

## 五、关键Bug修复记录

### V1.6 核心修复（2026-05-28）

#### 5.1 致命Bug: is_already_in_branch未赋值 ⭐⭐⭐

**问题：** 分支内移动时`is_already_in_branch`变量未设置为True，导致走到"兜底处理"逻辑

**影响：**
- 分支1结束错误进入分支2（而非出口18）
- 分支2结束位置计算错误（73号超出范围）
- 所有分支离开操作都异常

**根本原因：**
```python
# 错误代码（main.py:2264）
elif in_branch or self.engine.in_branch:
    # 缺失这行代码！
    # is_already_in_branch = True  ← 应该在这里赋值
    target_branch_key = self.engine.in_branch
```

**修复方案：**
```python
# 修复后（main.py:2265, 2606）
elif in_branch or self.engine.in_branch:
    is_already_in_branch = True  # ✅ 关键修复
    if self.engine.in_branch:
        target_branch_key = self.engine.in_branch
```

#### 5.2 steps_to_exit公式错误

**问题：** 公式多+1导致分支末尾误判为还能移动

```python
# 错误公式
steps_to_exit = branch_end - start_pos + 1  # 多算1步

# 正确公式
steps_to_exit = branch_end - start_pos  # 精确计算
```

#### 5.3 分支进入时跳格问题

**问题：** 在分支入口投1点会走2格

**修复：** 进入分支时补偿-1步
```python
if global_pos < branch_start:
    target_global_pos = start_pos + dv - 1  # 补偿多算的1步
else:
    target_global_pos = start_pos + dv
```

#### 5.4 done()回调覆盖正确位置

**问题：** 动画结束后done()函数错误地重新计算并覆盖棋子位置

**修复原则：** animate_roll()已经正确设置piece_pos_idx，done()只处理状态更新，不重复计算位置

---

## 六、当前待优化项 📋

### 高优先级
- [ ] 起点位置修正（独立凸出点，不在循环路径上）
- [ ] 70抽变格状态显示（UI提示用户已激活变格）
- [ ] 打包脚本完善（版本管理、输出目录优化）

### 中优先级
- [ ] 快捷键支持（Space/Enter单抽，Ctrl+R重置）
- [ ] 日志区域性能优化（限制最大行数）
- [ ] 存档功能（防止进度丢失）

### 低优先级（长期规划）
- [ ] 代码模块化重构（main.py超过2000行）
- [ ] 统计分析功能（概率分布图、保底趋势图）
- [ ] 主题切换系统（浅色/深色主题）
- [ ] 音效系统（抽卡音效、获得S级特殊音效）
- [ ] 图表可视化（matplotlib绘制统计图表）

---

## 七、技术债务记录 ⚠️

| 项目 | 影响 | 建议处理时机 |
|------|------|------------|
| main.py超过2000行 | 维护困难 | V2.0重构时处理 |
| 无单元测试 | 回归风险 | V2.0重构时补充 |
| 无错误日志系统 | 排错困难 | 下个版本添加 |

---

## 八、版本演进记录

| 版本 | 主要更新 | 状态 |
|------|---------|------|
| V1.0-V1.3 | 基础功能实现 | ❌ 已废弃 |
| V1.4 | CSV形状 + txt顺序双文件方案 | ⚠️ 临时方案 |
| **V1.5** | **单个CSV文件数据源** | ✅ 当前使用 |
| **V1.6** | **分支系统完善 + Bug修复** | ✅ **最新版本** |

---

> **文档最后更新**: 2026-05-28
> **当前版本**: V1.6
> **下次更新**: 待定（根据用户反馈）
